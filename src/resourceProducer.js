'use strict';

class ResourceProducer {
  constructor(input, output, location) {
    this.input = input;
    this.output = output;
    this.location = location;
  }
  tick() {
  }
}

module.exports = ResourceProducer;
